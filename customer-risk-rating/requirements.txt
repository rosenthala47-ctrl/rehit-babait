# ── ליבה (חובה) ──────────────────────────────────────────────
pandas>=2.0          # קריאה ומיזוג של טבלאות הנתונים
PyYAML>=6.0          # טעינת מודל הניקוד (scoring_model.yaml)
openpyxl>=3.1        # תמיכה בקבצי Excel (.xlsx) בנוסף ל-CSV

# ── ממשק Web (אופציונלי) ─────────────────────────────────────
# להרצת השרת:  uvicorn risk_rating.api:app --reload
fastapi>=0.110
uvicorn>=0.29

# ── חוות דעת מבוססת-AI (אופציונלי) ───────────────────────────
# דורש הגדרת ANTHROPIC_API_KEY
anthropic>=0.40

# ── פיתוח / בדיקות (אופציונלי) ───────────────────────────────
pytest>=7.0
